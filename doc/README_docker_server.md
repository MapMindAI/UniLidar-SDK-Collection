# Remote Web Control

The remote web control document moved to
[`doc/RemoteWebControl/README.md`](RemoteWebControl/README.md).

Use that document for:

- compose stack layout
- web UI and JSON API
- environment variables
- boot-time systemd setup
